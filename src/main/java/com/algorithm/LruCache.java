package com.algorithm;

/**
 * 简单版本的LRU(Least Recently Used) Cache 算法实现. 
 * LRU Cache应该满足下面的条件:
 * -LRU Cache有一定的容量,当小于这个容量时,可以往cache中正常添加数据.
 * -如果添加之后会超过容量, 
 * 那么需要把最近不被访问的元素淘汰掉.
 * 也就是说LRU Cache中应该保持的是最近被使用的元素. 
 * 请尝试实现一个简单版本的LRU Cache, 满足上面的条件.
 *
 * @author lk
 * @version 1.0
 * @date 2020/11/15 21:46
 */
public class LruCache {}
